window.Circadian=(()=>{const Z={ALA:"Asia/Almaty",NQZ:"Asia/Almaty",ICN:"Asia/Seoul",AYT:"Europe/Istanbul",FRA:"Europe/Berlin",CAN:"Asia/Shanghai",HKT:"Asia/Bangkok"};const off=(zone,date)=>{let p=new Intl.DateTimeFormat("en-US",{timeZone:zone,timeZoneName:"shortOffset",hour:"2-digit"}).formatToParts(new Date(date+"T12:00:00Z")).find(x=>x.type==="timeZoneName")?.value||"GMT";let m=p.match(/GMT([+-])(\d{1,2})(?::(\d{2}))?/);return m?(m[1]=="-"?-1:1)*(+m[2]+(+m[3]||0)/60):0};const unique=a=>[...new Set(a)];function get(day,days,ctx,fast){let airports=day?.airports||[],dest=airports.at(-1)||"ALA",zone=Z[dest]||"Asia/Almaty",delta=off(zone,day?.date||new Date().toISOString().slice(0,10))-off("Asia/Almaty",day?.date||new Date().toISOString().slice(0,10));let prev=days.filter(x=>x.date<day?.date&&x.airports?.at(-1)!==dest).at(-1),gap=prev?Math.max(0,Math.round((new Date(day.date)-new Date(prev.date))/864e5)):0,rate=delta>0?1:1.5,adapt=Math.min(Math.abs(delta),gap*rate),body=delta-Math.sign(delta)*adapt,kind=ctx==="auto"?(day?.kind||"rest"):ctx,h=24-fast,start=720,note="Ровное окно поддерживает цель без лишнего вечернего перекуса.";if(kind==="early"){start=540;note="Ранний duty: окно сдвинуто раньше, а сон остаётся приоритетом."}if(kind==="night"){start=840;note="Ночной duty: не продлевай голодание ценой бодрствования и сна."}if(kind==="training"){start=660;note="Тренировка: предусмотрено питание для восстановления."}if(kind==="recovery"){start=630;note="Восстановление: сон и мягкое возвращение к режиму важнее строгости."}if(Math.abs(body)>=2){start+=body>0?-60:60;note+=" Время тела отличается от местного на "+Math.abs(body).toFixed(1)+" ч; окно скорректировано постепенно."}return{zone,dest,delta,body,adapt,start,h,note,kind,airports:unique(airports)}}return{get,Z}})();