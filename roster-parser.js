window.FuelParser={async parse(file){const p=await import("https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.mjs");p.GlobalWorkerOptions.workerSrc="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.mjs";const pdf=await p.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise,page=await pdf.getPage(1),c=await page.getTextContent(),a=c.items.map(i=>({t:i.str.trim(),x:i.transform[4],y:i.transform[5]})).filter(i=>i.t),year=(a.map(i=>i.t).join(" ").match(/\b20\d{2}\b/)||[new Date().getFullYear()])[0];let ds=a.filter(i=>/^\d{2}\/\d{2}$/.test(i.t)),bins={};ds.forEach(i=>{let k=Math.round(i.y/3)*3;(bins[k]??=[]).push(i)});let row=Object.values(bins).sort((x,y)=>y.length-x.length)[0]||[];row=row.sort((x,y)=>x.x-y.x);if(row.length<20)throw Error("Не найдена строка дат Air Astana");let hy=row[0].y,cols=row.map((d,n)=>({d:d.t,x:d.x,l:n?((row[n-1].x+d.x)/2):-Infinity,r:n<row.length-1?((d.x+row[n+1].x)/2):Infinity}));return cols.map(col=>{let lines=a.filter(i=>i.x>=col.l&&i.x<col.r&&i.y<hy-10&&i.y>hy-300).sort((u,v)=>v.y-u.y).map(i=>i.t);let text=lines.join(" ");let code=(text.match(/\b(OFF|ROFF|HOMS|HOMX|AVLB|\d{3,4}A?)\b/i)||["","Duty"])[1].toUpperCase(),times=[...text.matchAll(/\b([01]\d|2[0-3]):[0-5]\d\b/g)].map(m=>m[0]);let cls=/OFF|ROFF|HOMS|HOMX|AVLB/.test(code)?"rest":times.some(t=>+t.slice(0,2)>=22||+t.slice(0,2)<=5)?"night":times.some(t=>+t.slice(0,2)<=8)?"early":"duty";return{date:`${year}-${col.d.slice(3)}-${col.d.slice(0,2)}`,code,times,kind:cls,detail:text.slice(0,300)}})}};